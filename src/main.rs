// main function is special: it is always the first code that runs in every executable Rust program.
fn main() {
    // Rust not using tab, using four space instead.
    println!("Hello, world!"); // '!' keyword is macro, not function.
}